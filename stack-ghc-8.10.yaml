# This snapshot pins the oldest supported GHC version and the lower
# bounds of all dependent packages.

resolver: lts-17.0

flags:
  aeson-schemas:
    run-integration-tests: true

ghc-options:
  '$locals': -Werror
